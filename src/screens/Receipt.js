import React from 'react';
import {Fix, Container, BakiyeText} from '../components/main/StyledComponents';

const Receipt = () => {
  return (
    <Fix>
      <Container>
        <BakiyeText>Receipt</BakiyeText>
      </Container>
    </Fix>
  );
};

export default Receipt;
