import React from 'react';
import {Fix, Container, BakiyeText} from '../components/main/StyledComponents';

const Pay = () => {
  return (
    <Fix>
      <Container>
        <BakiyeText>Pay</BakiyeText>
      </Container>
    </Fix>
  );
};

export default Pay;
